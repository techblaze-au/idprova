# Plan: Track E — Infrastructure & CI/CD

**Phase:** 1 of 1
**Plan:** 01-01
**Scope:** `.github/`, `Dockerfile`, `docker-compose.yml`, `scripts/`

## Tasks

### Task 1: CI Pipeline — Build & Test
Create/update `.github/workflows/ci.yml`: matrix build (stable + 1.75 MSRV), cargo test --workspace (exclude SDK crates), cargo clippy, cargo fmt --check. Cache cargo registry + target dir. Trigger on push to main and PRs.

**Files:** `.github/workflows/ci.yml`
**Verify:** YAML is valid, references correct workspace excludes

### Task 2: CI Pipeline — Security Audit
Add `cargo audit` step to CI. Create `.github/workflows/audit.yml` — runs on schedule (weekly) + on Cargo.lock changes. Uses `rustsec/audit-check` action.

**Files:** `.github/workflows/audit.yml` (new)
**Verify:** YAML is valid

### Task 3: Docker Build Optimization
Improve existing `Dockerfile`: multi-stage build, cargo-chef for layer caching, non-root user, health check (`/health` endpoint), minimal final image (distroless or slim). Build only the registry binary.

**Files:** `Dockerfile`
**Verify:** `docker build -t idprova-registry .` (if docker available, otherwise validate Dockerfile syntax)

### Task 4: Docker Compose Stack
Create `docker-compose.yml`: registry service with SQLite volume mount, optional Caddy reverse proxy service with automatic HTTPS, environment variable configuration.

**Files:** `docker-compose.yml` (new)
**Verify:** `docker compose config` (if available, otherwise validate YAML)

### Task 5: Release Workflow
Create `.github/workflows/release.yml`: triggered on tag push (`v*`), build release binaries (Linux x86_64, macOS x86_64+aarch64, Windows x86_64), create GitHub release with artifacts, build+push Docker image to GHCR.

**Files:** `.github/workflows/release.yml` (new)
**Verify:** YAML is valid, matrix covers all targets

### Task 6: Developer Scripts
Create helper scripts: `scripts/dev-setup.sh` (install deps, build, run tests), `scripts/run-registry.sh` (start registry with default config). Make executable.

**Files:** `scripts/dev-setup.sh` (new), `scripts/run-registry.sh` (new)
**Verify:** Scripts are executable, use correct paths

## Verification

After all tasks:
```bash
cargo build --workspace
cargo test --workspace
# Validate all YAML files
```

## Completion

When all 6 tasks pass verification:
1. Update HANDOVER.md with final status
2. `touch .planning/TRACK_COMPLETE`
3. Exit
