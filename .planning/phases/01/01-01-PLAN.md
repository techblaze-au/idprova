# Plan: Track B — Registry Hardening

**Phase:** 1 of 1
**Plan:** 01-01
**Scope:** `crates/idprova-registry/`

## Tasks

### Task 1: Health & Readiness Endpoints
Add `GET /health` (returns 200 + JSON status) and `GET /ready` (checks SQLite connection, returns 200/503).

**Files:** `crates/idprova-registry/src/main.rs`
**Verify:** `cargo test -p idprova-registry`, `curl` test in doc comment

### Task 2: Structured Error Responses
Replace string error responses with JSON error bodies: `{"error": "...", "code": "...", "request_id": "..."}`. Create `error.rs` in registry crate with `ApiError` type implementing `IntoResponse`.

**Files:** `crates/idprova-registry/src/error.rs` (new), `crates/idprova-registry/src/main.rs`
**Verify:** `cargo test -p idprova-registry`

### Task 3: CORS Configuration
Enable CORS via `tower-http::cors` — allow configurable origins (default: `*` for dev), standard headers, GET/POST/OPTIONS methods.

**Files:** `crates/idprova-registry/src/main.rs`
**Verify:** `cargo test -p idprova-registry`

### Task 4: Request Size Limits
Add `tower-http::limit::RequestBodyLimitLayer` — 1MB default for all endpoints.

**Files:** `crates/idprova-registry/src/main.rs`
**Verify:** `cargo test -p idprova-registry`

### Task 5: Input Validation
Add validation for AID registration: check DID format (`did:idprova:*`), public key length (32 bytes), required fields present. Return 400 with structured errors for invalid input.

**Files:** `crates/idprova-registry/src/main.rs`, `crates/idprova-registry/src/store.rs`
**Verify:** `cargo test -p idprova-registry` — add test cases for invalid input

### Task 6: Request Tracing
Add `tower-http::trace::TraceLayer` with request ID generation (ULID). Log method, path, status, duration for every request.

**Files:** `crates/idprova-registry/src/main.rs`
**Verify:** `cargo test -p idprova-registry`

### Task 7: DAT Revocation Endpoint Enhancement
Improve `POST /revoke` — validate DAT signature before accepting revocation, return structured error if invalid. Add `GET /revocations` list endpoint with pagination (limit/offset query params).

**Files:** `crates/idprova-registry/src/main.rs`, `crates/idprova-registry/src/store.rs`
**Verify:** `cargo test -p idprova-registry` — add integration tests

## Verification

After all tasks:
```bash
cargo test --workspace
cargo clippy --workspace -- -D warnings
```

## Completion

When all 7 tasks pass verification:
1. Update HANDOVER.md with final status
2. `touch .planning/TRACK_COMPLETE`
3. Exit
